<!DOCTYPE html>
<html>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Type the full url</h1>
    <input type="text" id="oldurl"/>
    <button type="button" id="btn">OK</button>
  <div id="newid"></div><html>
<script>
$(document).ready(function() {
    $('#btn').click(function () {
      var data={
      	'originalurl' : $("#oldurl").val()
      	
      };
      console.log(data);
      	$.ajax({
        type:"POST",
        url:"/form",
        data :  data, 
        //dataType: 'json',  // multiple data sent using ajax
        success: function (result) {
           alert(result);
          var out = result;
                $("#newid").html(out);
           //window.location = '/display';
        }
        });
    });
});
</script>